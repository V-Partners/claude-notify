# VAPID Keys - Auto-generated on first run via setup wizard
# Or generate manually: python -c "from py_vapid import Vapid; v=Vapid(); v.generate_keys(); print(f'VAPID_PRIVATE_KEY={v.private_pem().decode()}'); print(f'VAPID_PUBLIC_KEY={v.public_key_urlsafe_base64()}')"
VAPID_PRIVATE_KEY=
VAPID_PUBLIC_KEY=
VAPID_EMAIL=mailto:your-email@example.com

# Server configuration
HOST=0.0.0.0
PORT=5000
